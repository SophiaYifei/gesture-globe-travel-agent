flask>=3.0.0
Pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
openai>=1.30.0
torch>=2.0.0
transformers>=4.35.0

# DL perception — hand gestures (MediaPipe) and terrain CLIP.
mediapipe>=0.10.33
flask-socketio>=5.6.0   # installed for future WebSocket path; /gesture uses POST for now
