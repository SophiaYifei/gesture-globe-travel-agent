# Copy to .env and fill in your keys. .env is gitignored.

# OpenRouter — drives the ReAct agent (DL planner) and LLM-as-judge eval.
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx

# Google Places API (New) — powers search_places tool for real POI data.
GOOGLE_PLACES_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxx

# (Legacy) Anthropic direct SDK — no longer used after the OpenRouter unification,
# but kept as reference in case we fall back to Tiffany's original planner path.
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx
